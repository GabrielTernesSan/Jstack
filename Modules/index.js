/*
const modulo = require('./printName');
const { printName, lastName } = require('./printName');

 modulo.printName(`Gabriel ${modulo.lastName}`);
printName(`Gabriel ${lastName}`);
*/

// Módulos nativos
const os = require('os');

console.log(os.type());